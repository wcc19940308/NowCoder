package NowCoder.advanced_class06;

/**
 * 求未排序数组中累加和小与或等于给定值的最长子数组长度（数组中元素可正、可负、可0）
 * min_sum[i]代表i开头的所有子数组的最小累加
 * min_sum_index[i]代表i位置取得最小累加的右边界
 * 只关心更长的子数组，右边界不回退，舍弃无效答案 c
 */
public class LongestSumSubArrayLengthInArray {
    public int getMaxLength(int[] arr, int aim) {
        return 0;
    }
}
